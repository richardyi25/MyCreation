/*
Richard Yi
Background #1
Mrs. Krasteva
Oct. 19 2016


This program...
*/

import hsa.Console;
import java.awt.*;

public class Background1 
{
    private Console c;
    
    public Background1 (Console con)
    {
        c = con;
        draw();
    }

    private void draw ()
    {
        Color sand = new Color(255, 233, 121);
		Color sunset = new Color(176, 109, 50);
		Color dorito = new Color(247, 111, 0);
		Color sun = new Color(255, 255, 0);
		Color deadBush = new Color(153, 76, 0);
		Color skull = new Color(255, 240, 190);
		
        c.setColor(sunset);
		for (int x = 0; x <= 640; x++){
			c.drawLine(x, 0, x, 180);
		}

        c.setColor(sand);
        for(int x = 0; x <= 640; x++){
            c.drawLine(x, 180, x, 500);
        }

        c.setColor(sun);
        for(int i = 0; i <= 80; i++){
            c.drawOval(320 + i, 120 + i, i * 2, i * 2);
        } 
    }

}
